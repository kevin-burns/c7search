# Binaries
/c7search
/c7search.exe
/dist/

# Test / coverage artefacts
/coverage.out
/coverage.html
/cover.out
/cover.html
/.coverage/

# Local scratch — API probe captures, design notes, throwaway scripts
/.notes/

# IDE / OS noise
.vscode/
.idea/
.DS_Store

# Per-user Claude Code state (permissions + locally-installed skill copy).
# The canonical SKILL.md lives at skills/c7search/SKILL.md.
.claude/

# Project conventions for AI agents (per-developer, may contain personal
# paths like Ogham binary location). Auto-loaded by Claude Code from the
# working tree; not part of the public artifact.
/CLAUDE.md
